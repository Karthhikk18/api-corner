# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secrets (Change this to a strong secret string in production)
JWT_SECRET=api_corner_super_secret_key_12345

# Database Connection (Leave empty to use local JSON file database fallback)
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxx.mongodb.net/api_corner
